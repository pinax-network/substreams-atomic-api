{
  "name": "substreams-atomicmarket-api",
  "description": "Atomicmarket sales API powered by Substreams",
  "version": "v0.1.0",
  "repository": "https://github.com/pinax-network/substreams-atomicmarket-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "bun run tsc --noEmit --skipLibCheck --pretty",
    "test": "bun test src/tests/*.spec.ts --coverage",
    "build": "bun build --compile ./src/index.ts --outfile substreams-atomicmarket-api"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.7.2",
    "@sinclair/typebox": "^0.31.17",
    "dotenv": "^16.3.1",
    "hono": "^3.7.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "bun-types": "^0.6.2",
    "typescript": "^5.2.2"
  }
}