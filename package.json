{
  "name": "substreams-atomic-api",
  "description": "Atomic API",
  "version": "v0.2.0",
  "homepage": "https://github.com/pinax-network/substreams-atomic-api",
  "type": "module",
  "author": {
    "name": "Paul Meignan",
    "email": "paulm@pinax.network",
    "url": "https://github.com/Pelotfr"
  },
  "scripts": {
    "start": "bun index.ts",
    "dev": "bun --watch index.ts",
    "pretest": "bunx tsc --noEmit --skipLibCheck --pretty",
    "test": "bun test --coverage",
    "build": "bun build --compile ./index.ts --outfile substreams-atomic-api"
  },
  "dependencies": {
    "@clickhouse/client-web": "latest",
    "@wharfkit/antelope": "latest",
    "commander": "latest",
    "dotenv": "latest",
    "openapi3-ts": "latest",
    "prom-client": "latest",
    "tslog": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "latest"
  }
}